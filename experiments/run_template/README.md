# Run Directory Template

Run directories are created automatically by `scripts/manage_experiment.py`.
They contain a resolved configuration snapshot, metadata, append-only metrics,
checkpoints, logs, and artifacts. Do not manually commit generated run
directories.

